# คำตัดสิน K3w_one_contains_other — 2026-08-26

operator ตัดสินครบทุกกลุ่ม · บันทึกไว้เพื่อไม่ให้เซสชันถัดไปมารื้อซ้ำ
เกต `check:keyword-collisions` **เสนออย่างเดียว ไม่เขียน `target_keyword_fp`** — ทุกอย่างในนี้เป็นการตัดสินของคน

## ยอมรับตามเดิม ไม่ขยับ (17 คู่)

**กลุ่ม A · `X` (บริการ §3) vs `X ราคา` (หน้าราคา §5.13.1) — 9 คู่**
`อุดฟัน` `ถอนฟัน` `ครอบฟัน` `รักษารากฟัน` `รากฟันเทียม` `รักษาโรคเหงือก` `วีเนียร์` `จัดฟันใส` `ขูดหินปูน`

**เหตุผล:** หน้าบริการเป็นเจ้าของชื่อบริการ หน้าราคาเป็นเจ้าของคำถามราคา = การออกแบบที่ตั้งใจ ไม่ใช่การชนกัน

⚠️ `ขูดหินปูน ราคา` (vol 15,350) มี volume **สูงกว่า** `ขูดหินปูน` (12,325) ทั้งที่หน้าราคาเป็น tier C
ส่วนหน้าบริการเป็น tier A · operator ตัดสินว่าไม่ขยับ — ถ้าอนาคตอยากทบทวน จุดนี้คือจุดที่ข้อมูลไม่เข้ากับผัง

**กลุ่ม B · hub กับลูกในโซนเดียวกัน — 2 คู่** `ครอบฟัน`/`ครอบฟัน emax` · `ดมยาสลบทำฟัน`/`ดมยาสลบทำฟันเด็ก`
**กลุ่ม C · บริการ §3 vs ผลงาน §7 — 3 คู่** — คนค้น "รีวิว" คนละเจตนากับคนค้นชื่อบริการ
**กลุ่ม E · แบรนด์/ทำเล — 2 คู่** `สไมล์สเคป`/`ติดต่อ สไมล์สเคป` · `รากฟันเทียม ศรีนครินทร์`/`คลินิกรากฟันเทียม ศรีนครินทร์`

## เปลี่ยนจริง (กลุ่ม D)

| หน้า | เดิม | ใหม่ |
|---|---|---|
| **3.10.1.3** | target `invisalign ดีไหม` (vol 0) · ชื่อ "จัดฟันใส Thermoformed — Conventional Clear Aligner — 3rd option (R16)" | target **`invisalign`** (vol 6,100) · ชื่อ **"จัดฟันใส invisalign"** · `invisalign ดีไหม` ลงเป็น semantic |
| **4.6.2** | target `invisalign` | **ว่าง** — operator เสนอ `invisalign aligner` แต่คำนั้นไม่มีวัดไว้ จึงใช้ทางเลือก "ไม่ต้องใส่" ที่ operator อนุญาต |
| **3.5.4** | "ฟันปลอม — Removable Denture (R18 ย้ายจาก General)" | **"ทำฟันปลอม"** (target `ฟันปลอม` ไม่เปลี่ยน) |
| **6.2.4.10** | "ฟันปลอม — ประเภทและการดูแล" | **"วิธีดูแลฟันปลอม ทำยังไงให้อยู่ได้นาน"** (target `ดูแลฟันปลอม` ไม่เปลี่ยน) |
| **1 (HOME)** | semantic 3 คำ | +`สไมล์สเคป คลินิก` +`smile scape` = 5 คำ · target `สไมล์สเคป` ถูกอยู่แล้ว |

⚠️ `uq_page_master_target_keyword_fp` บังคับว่าคีย์เป็น target ได้หน้าเดียว — ต้องปล่อยจาก 4.6.2 ก่อนให้ 3.10.1.3 รับ

## 🔴 ค้างเพราะคีย์ไม่มีวัดไว้ — ห้าม hand-insert

| คำที่ operator ขอ | หน้า | สถานะ |
|---|---|---|
| `invisalign aligner` | 4.6.2 | ไม่มีในตารางคีย์ของแบรนด์ไหนเลย |
| `smile scape clinic` | 1 (HOME) | ไม่มี (มีแต่ `smile scape` เฉย ๆ ซึ่งใส่ให้แล้ว) |
| ~~`Smilescape dental clinic มีกี่สาขา`~~ | 8.1 | ✅ **แก้แล้ว** — operator ยืนยันว่าพบใน PAA จริง |

ถ้าจะใช้จริง ต้องวัด volume/intent เข้าตารางคีย์เวิร์ดก่อน แล้วค่อยผูก

## เพิ่มคีย์จาก PAA (2026-08-26)

`smilescape dental clinic มีกี่สาขา` → target ของ **8.1** · คีย์เดิม `ติดต่อ สไมล์สเคป` ย้ายลงเป็น semantic

| ฟิลด์ | ค่า | เหตุผล |
|---|---|---|
| `predicted_serp_features` | `People Also Ask` | บันทึกที่มาไว้ในฟิลด์ที่ตรงความหมาย |
| `search_intent` | `navigational` | แบรนด์ + entity `smilescape-dental-clinic` เหมือน `ติดต่อ สไมล์สเคป` |
| **volume** | **ไม่ใส่** | 🔴 การโผล่ใน PAA เป็นหลักฐานว่า**มีคนถาม** ไม่ใช่ค่า**ว่ากี่คน** — ไม่สร้างแถวใน snapshot |
| `qualitative_kd` | ว่าง | ยังไม่ได้วัด |

keyword ปรับเป็นตัวเล็กตามคอนเวนชันของตาราง (operator เขียนว่า "Smilescape...")

**ตรวจแล้วว่าไม่สร้างการชนใหม่** — `smilescape` (target ของ 2.1) เป็นสตริงย่อยของคีย์ใหม่จริง
แต่อัตราส่วน **0.323 < `CONTAIN_MIN_RATIO` 0.6** เกตจึงไม่นับเป็นการชน
คอมเมนต์ในโค้ดอธิบายเคสนี้ตรง ๆ: *"Below this it is a head term and its own long tail"*
`smilescape` = head term · คำ PAA = long tail ของมัน = คนละดีมานด์ **threshold ทำงานถูก ไม่ใช่บั๊ก**
