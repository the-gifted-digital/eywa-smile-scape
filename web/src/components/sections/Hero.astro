---
import Button from '~/components/ui/Button.astro';
import Image from '~/components/ui/Image.astro';
interface Props {
  eyebrow: string; title: string; body: string;
  primaryCta: { label: string; href: string };
  secondaryCta: { label: string; href: string };
  image: { src?: string; alt: string; label?: string };
}
const { eyebrow, title, body, primaryCta, secondaryCta, image } = Astro.props;
---
<section data-hero class="bg-brand-anchor text-brand-neutral-0 relative overflow-hidden">
  <div class="hero-sheen" aria-hidden="true"></div>
  <div class="max-w-6xl mx-auto px-4 py-16 md:py-24 grid md:grid-cols-2 gap-10 items-center relative z-10">
    <div data-reveal-stagger>
      <p data-reveal class="font-display text-sm font-medium uppercase tracking-[0.2em] text-brand-highlight">{eyebrow}</p>
      <h1 data-reveal class="mt-4 text-4xl md:text-5xl font-bold leading-tight">{title}</h1>
      <p data-reveal class="mt-6 max-w-xl text-lg text-brand-neutral-200">{body}</p>
      <div data-reveal class="mt-8 flex flex-wrap gap-4">
        <Button href={primaryCta.href} variant="primary">{primaryCta.label}</Button>
        <Button href={secondaryCta.href} variant="outline" class="!border-brand-neutral-0/40 !text-brand-neutral-0 hover:!bg-brand-neutral-0/10">{secondaryCta.label}</Button>
      </div>
    </div>
    <div data-parallax="0.06">
      <Image src={image.src} alt={image.alt} label={image.label} width={640} height={480} loading="eager" />
    </div>
  </div>
</section>
