---
// GTM <head> snippet, loaded off the main thread via Partytown.
// Renders nothing until PUBLIC_GTM_ID is set (skeleton-safe).
import { GTM_ID, analyticsEnabled } from '~/lib/analytics';
---
{
  analyticsEnabled && (
    <script type="text/partytown" is:inline set:html={`
      (function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':new Date().getTime(),event:'gtm.js'});
      var f=d.getElementsByTagName(s)[0],j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';
      j.async=true;j.src='https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
      })(window,document,'script','dataLayer','${GTM_ID}');
    `} />
  )
}
