# SmileScape — Session Handover (2026-06-07)

Context for spinning up **2 parallel sessions**: (A) Cloudflare image/video storage, (B) Supabase real schema + data load. Plus a snapshot of the web/LP work just completed.

---

## ⏩ SESSION A — PROGRESS UPDATE (2026-06-07, later)

**Scope pivot (operator):** do NOT migrate the MVP LP assets — they stay as committed static assets. Session A now = **design + build the production media/DAM pipeline** for the 722-page site. Full spec: **`docs/SESSION-A-MEDIA-PIPELINE.md`**. New decisions: **SS-DR-015** (pipeline) + **SS-DR-016** (patient-consent lifecycle, L2 enforcement) in `docs/decision-records.md`.

**Architecture (locked):** Notion "Media Library" (human authoring) → **n8n** (sync) → **Cloudflare R2** (`smilescape-media`, `cdn.smilescapeclinic.com` + Image Transformations) → **Supabase `seo_media_assets`** (app source of truth) → Astro reads `seo_servable_media` view at build. Patient images carry a consent lifecycle; a daily n8n sweep quarantines the R2 object + purges cache + flips status when consent lapses (PDPA backstop, independent of static build).

**Done this session:**
- ✅ **Notion Media Library DB** built on **notion-marketing-vt** under *Knowledge Graph* — db `656514e1-274f-4ea5-8aab-576d66858a27`, data source `collection://20e248f7-4b65-44eb-bfd4-90dac48a79d7`. 27 props (incl. `Brand`→[DB 1.1] Brand Database, `Entity`→Entity Graph relations; `Consent Check` + `Days Left` formulas) + 4 views + 2 `[TEMPLATE]` rows.
- ✅ **`seo_media_assets` DDL draft** matching GTGT Spec v1.22 conventions (DR-008 identity, FK→seo_entity_graph/brands/seo_branches/seo_authors_reviewers/seo_website_page_master, RLS, audit, `seo_servable_media` view): **`docs/sql/seo_media_assets.draft.sql`** — review-only, route apply via Session B (DR-037/spec v1.23 proposed).
- ✅ **n8n workflows (multi-account, scaffold):** `docs/n8n/wf1-media-sync.json`, `wf2-media-expiry-sweep.json`, `README.md`. Single account-agnostic workflows; route by `notion_workspace` + token-by-expression (`$vars['NOTION_TOKEN_'+workspace]`). Adding an account = 1 CONFIG row + 1 variable.

**Key discovery:** Supabase **"GTGT"** (`lffcbeszjqzioobqfdav`) is a mature Spec v1.22 SEO schema (~40 `seo_*` tables, Session B). **marketing-vt Notion ↔ GTGT Supabase = the same N↔S system.** ⚠️ RLS disabled on `seo_brand_centers` + 2 `_archive_*` tables (pre-existing; surface to Session B).

**Blocked on operator:**
1. **R2 not enabled** (Cloudflare dashboard) → blocks bucket create + cdn domain + Transformations.
2. **notion-gifted** ("The Gifted Synapse"): integration `GIFTED X CLAUDE` has **no shared pages** + no Entity Graph/Brand DB + raw-API create is broken → to replicate the Media Library there: share a parent page, connect the **official Notion MCP** to gifted, decide Entity/Brand relations.
3. **n8n deploy:** `n8n-mcp` server was NOT connected this session → workflows delivered as importable JSON. To auto-deploy next time: reconnect n8n-mcp, or provide an n8n API key for REST deploy, or import the JSON manually.

**Next (unblocked):** Astro data layer reads `seo_servable_media` + srcset helper via `/cdn-cgi/image/`. **Next (after apply):** Session B applies `seo_media_assets`; set n8n Variables/Credentials; enable R2.

---

## 0) TL;DR — current state

> **2026-06-12 — `go.` declared the temporary production domain ([SS-DR-017]).** The Astro site — homepage + 2-tier header/mega-nav + light footer + implant-check assessment + the dental-implant LP — is live on `go.smilescapeclinic.com` and treated as **real production** while WordPress keeps the apex. Whole domain is **noindex** via two layers: per-page `<meta robots>` default + a host-scoped `X-Robots-Tag: noindex` belt in `worker/index.ts` (self-disables at apex cutover). Apex cutover is a documented checklist in SS-DR-017 — **not yet done**.

- **LIVE Google-Ads landing page**: `https://go.smilescapeclinic.com/lp/dental-implant/` (Astro → Cloudflare Workers Static Assets).
- **Parallel-launch strategy in effect**: WordPress still serves the apex `smilescapeclinic.com`; the new Astro site is on the **`go.` subdomain** (no downtime). At full cutover, point apex → the same Astro worker.
- **Design system locked** (Bridge palette + Google Sans / Cabinet Grotesk). Tokens in `design/brand-foundation/tokens.json`.
- **Tracking live**: GTM `GTM-NFBVZT43` (GA4 `G-LJEPCPC199`, property `487674900`). dataLayer events: `lead_submit`, `line_click`, `call_click`.
- **Lead form** posts to n8n webhook `https://nexorcus.app.n8n.cloud/webhook/smilescape-website-lead-form`.
- Key contacts used on site: phone **092 293 6226** (`tel:+66922936226`), LINE **https://maac.io/6yp2p**.

### Repo / deploy
- Repo: `/Volumes/SSD NN/CLAUDE AI/repos/brands/eywa-smile-scape`
- Branch: **`web-skeleton`** (the Astro app lives in `web/`).
- Deploy: `cd web && npm run build && npx wrangler deploy` (wrangler authed this session; custom domain `go.smilescapeclinic.com` already provisioned via `web/wrangler.jsonc` routes).
- Node 22, npm. Astro 4 + Tailwind. Fonts self-hosted (no Google Fonts CDN).

### What's on the LP (sections, top→bottom)
Hero (cover img + offer 29,900฿) → trust bar → **before/after gallery** (7 imgs, lightbox, 1st full-width on mobile) → Why Blue Diamond → **Blue Diamond highlight** (img float + MegaGen) → what-is + **animated component callouts** → offer band → **process 5 steps** (imgs) → **implant types** (3 imgs) → indications/benefits/limitations → **doctor cross-fade** (หมอแฮม↔หมอแพรว, full-bleed mobile) → text reviews → **video review** (square) → where-to-choose checklist → FAQ (FAQPage schema) → **appointment form** (n8n) + หมอแฮม cutout + badges → final CTA → footer (white logo). Sticky mobile CTA (reveals after hero; LINE green + pulse; phone icon rings). Privacy page at `/privacy-policy/`.

### Known open items (non-blocking)
- **Compliance review pending**: before/after photos + "รับประกันตลอดชีพ"/"ตัว Top" guarantee language vs Thai dental-advertising law + Google healthcare policy. Recommend the healthcare-marketing-compliance specialist before/while scaling ad spend. Clinic license number not yet shown (Q-Clinic # was a pending operator item).
- GTM triggers for the 3 dataLayer events must be wired inside the GTM container → Google Ads conversion tags (operator said the container is already connected).

---

## SESSION A — Cloudflare image/video storage

### Decision (already agreed with operator)
- **Cloudflare Images** — plan **"Images & Stream" ($0/mo pay-as-you-go)** — chosen because it has a **dashboard upload UI for the team** + auto resize/optimize/variants + CDN. Near-free at clinic scale. Start free.
- **Cloudflare Stream** for video (the review clip). Upgrade to the **$5 Starter Bundle** when video/volume grows.
- **R2** later for raw originals / large files / backups if needed.
- **DR-035**: Astro serves image binaries from Cloudflare (Images / R2 / Image Transformations); **Supabase stores only the URL**. Same principle applies to Cloudflare Images (Supabase keeps the delivered URL).

### Current state (what to migrate)
All LP media is **temporarily committed in the repo** and must move to Cloudflare:
- Images: `web/public/images/lp/` (~4.3 MB, ~30 files). Originals were pulled from `https://smilescapeclinic.com/wp-content/uploads/2025/04/…` (WP).
  - before/after: `review1-7` · process: `step1-5` · implant types: `case1-3` · `component1` · doctors: `doctor1` (หมอแฮม), `doctor-praew`, `doctor-haam-cutout` · hero cover: `clinic1.webp` (= ik.webp) · `bluediamond.png`, `megagen-logo.png`, `indication-case.jpg`, `video-poster.jpg` · testimonial avatars: `avatar1-8` (mostly unused) · logos: `logo-smilescape.png` (header), `logo-stacked.png` (footer) · `web/public/favicon.png`
- Video: `web/public/videos/review-clip.mp4` (**6.7 MB**, square 1280×1280, currently `preload=none` + poster) → **Cloudflare Stream**.

### TODO for Session A
1. Create/confirm Cloudflare Images (and Stream) on the account that owns the `smilescapeclinic.com` zone (same account used for the Workers deploy).
2. Upload the LP images → get delivery URLs (or set up Image Transformations + variants).
3. Upload `review-clip.mp4` → Stream → get embed/HLS + poster.
4. Replace `/images/lp/*` references in `web/src/pages/lp/dental-implant.astro` (+ logos in `web/src/layouts/Landing.astro`) with Cloudflare URLs. Keep `width`/`height` for CLS.
5. Decide the team workflow: who uploads, naming, and how the URL lands in Supabase (per DR-035).
6. Remove the heavy binaries from `web/public/` once migrated (keep repo lean).

> NOTE: fonts under `web/public/fonts/` (cabinet-grotesk, google-sans) are **self-hosted brand fonts — do NOT move to Cloudflare Images**.

---

## SESSION B — Supabase real schema + data load

### Decision (already agreed)
- **Supabase = source of truth.** **Adopt the protocol's pre-defined schema — do NOT reinvent.**
  - Spec: `Schema_Overview_EYWA_v1_21.md` (in `eywa-protocol-spec`) — 9 groups, ~41 tables (Brand&Org, Knowledge Architecture/entities/edges, Page System, Keyword Intelligence, Performance Facts, Backlinks, AI Ops & Embeddings/pgvector, Governance, Entity Extensions).
  - **Notion reflects body content** for human review (§18.5 Notion ↔ Supabase Field Mapping; DR-EYWA-MKT-006 / DR-EYWA-POLY-004).
- The schema supports BOTH Astro and WordPress consumption (confirmed earlier).

### Inputs (content plan — Stage 1, in `content-plan/`)
- `sitemap.md` — **722 pages**, 7-column model (#/Page Name/Layer L1-L7/Tier A-D/Funnel/Page Type/Primary Entity). Section 3 reordered to 14 categories. Fully operator-reviewed (R16-R26).
- `entities.md` — **166 entities** (12-column schema).
- `relationships.md` — entity edge graph, 10 edge types (DR-021 reciprocal-detection).
- `keyword-seed-list.md`, `patient-journey.md`, `egp-output-summary.md`, `competitor-scan.md`.
- ⚠️ **osstem → neodent** cleanup is DONE in entities/relationships/sitemap/seed-list (SS-DR-001: clinic does NOT sell Osstem; Neodent is the value-premium brand). Don't reintroduce Osstem as our product.

### Astro-side shape already scaffolded (forward-compatible)
- `web/src/content/config.ts` — `pages` + `articles` collections aligned to the sitemap 7-column model + relations + FAQ (the Astro reflection of the Supabase "Page System"). A build step can later hydrate these from Supabase.

### Tooling
- **Supabase MCP is connected** this session (server id `5814a0fb-eb09-4ece-89b3-227aeced0519`): `list_projects`, `apply_migration`, `execute_sql`, `list_tables`, `generate_typescript_types`, `search_docs`, etc.
- There is also a `supabase:supabase` skill + `supabase:supabase-postgres-best-practices`.

### TODO for Session B
1. Read `Schema_Overview_EYWA_v1_21.md` fully → write Supabase **migrations** for the ~41 tables (adopt as-is; pgvector for embeddings group).
2. Set up the Supabase project (confirm which project/org).
3. Load Stage-1 data: entities (166) → entity edges (relationships) → page system (722 pages) → keyword intelligence (seed list) → brand/org + 2 branches.
4. Wire the §18.5 Notion ↔ Supabase reflection for body-content review.
5. Image URLs (from Session A) populate the URL fields per DR-035.

---

## SESSION C — Homepage MVP (parallel-safe)

### Where it goes (no conflict with the live WP)
- Build the homepage at the **`go.` subdomain ROOT**: `web/src/pages/index.astro` → `https://go.smilescapeclinic.com/`.
- This is **already separate from the live WordPress** (which serves the apex `smilescapeclinic.com`). Editing `index.astro` does **not** touch the LP (`lp/dental-implant.astro`) — fully parallel-safe.
- The go. root currently shows a throwaway **skeleton placeholder** — replace it with the real homepage.
- Keep **`noindex,follow`** on go. pages for now (avoid Google indexing the temp subdomain as a duplicate of the future apex). Remove noindex at apex cutover.

### Reuse (don't rebuild from scratch)
- Same Astro project (`web/`), **Bridge tokens** (`design/brand-foundation/tokens.json`), fonts (Google Sans + Cabinet Grotesk, `font-display` for EN headings).
- The LP (`lp/dental-implant.astro`) is a rich component library to lift from: hero w/ cover image, section patterns, cards, **animated callouts**, **before/after lightbox**, **doctor cross-fade**, square **video**, the **n8n appointment-form embed**, **sticky mobile CTA**. Copy/adapt these.
- Tracking: reuse the GTM setup. NOTE the LP's GTM + dataLayer events live in **`Landing.astro`** (the nav-less LP shell). The homepage needs its **own full-site shell** with header **nav** + footer — either extend `Base.astro` (already has nav, but with placeholder links) or make a `Site.astro` layout. Re-add GTM (`GTM-NFBVZT43`) + the `line_click`/`call_click` listeners + form `lead_submit` to whatever shell the homepage uses.

### Inputs for homepage content/IA
- `content-plan/sitemap.md` — homepage + Section-1 (home/about/trust) structure within the 722-page plan.
- Existing WP homepage (for reference): scrape `https://smilescapeclinic.com/` — hero "Your Final Dental Implant Selection / Blue Diamond", services grid, branches, reviews. The WP nav: Home / Our Unique / Dental Implant / Blogs; services live under `/service/...` and `/team`, `/about`, `/contact`.
- Brand: `docs/brand-concept.md` ("The Lifetime Foundation"), Hero service = Blue Diamond 29,900฿, 2 branches (รัตนาธิเบศร์ MRT ม่วง / ศรีนครินทร์ MRT เหลือง), founders หมอแฮม + หมอแพรว (both Oral & Maxillofacial).
- Same compliance caveats as the LP (before/after + guarantee language).

### Deploy
- Same as LP: `cd web && npm run build && npx wrangler deploy`. Homepage ships at go. root automatically (it's `index.astro`). The LP + privacy + homepage are one deploy.

---

## Key DRs / refs
- **DR-EYWA-MKT-005** — Astro stack profile (the `web/` subfolder pattern; followed polyvex which is LIVE on Cloudflare Workers Static Assets).
- **DR-029** — Universal Brand Design System (tokens.json, DTCG). Bridge palette LOCKED.
- **DR-035** — image binaries on Cloudflare; Supabase stores URL only.
- **DR-021 / DR-022** — internal linking (reciprocal) / two-layer.
- **§18.5** — Notion ↔ Supabase field mapping.
- **SS-DR-001** — Implant brand strategy (Blue Diamond hero, Neodent value-premium, Straumann premium, Ceramic specialty; Osstem removed).
- Brand colors (Bridge): anchor `#14386B`, primary `#217DEA` (CTA/links use `primary-deep #1B66C2` for WCAG AA), highlight `#7BA4DD` (logo), accent `#EE9C9C` (coral), ice `#EAF3FB`, paper `#F5FAFE`, ink `#383838`.

---

## Deploy cheatsheet
```bash
cd "/Volumes/SSD NN/CLAUDE AI/repos/brands/eywa-smile-scape/web"
npm install            # first time
npm run build
npx wrangler deploy    # → go.smilescapeclinic.com (wrangler login if prompted)
# local preview: npm run preview → http://localhost:4321/ (NEVER open dist/ via file://)
```
